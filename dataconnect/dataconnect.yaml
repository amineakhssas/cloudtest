specVersion: "v1beta"
serviceId: "cloudp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cloudp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
